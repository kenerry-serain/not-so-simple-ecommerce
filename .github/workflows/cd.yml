on:
  workflow_run:
    workflows: [ci]
    types:
      - completed
      
jobs:
  test:
    name: Test on node 
    runs-on: ubuntu-latest
    steps:
    - uses: actions/download-artifact@v5
      with:
        name: ${{github.sha}}
    - name: Display structure of downloaded files
      run: ls -R